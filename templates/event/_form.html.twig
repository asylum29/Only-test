{{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="hidden" name="return" value="{{ return }}">
    <button class="btn btn-primary">{{ button_label|default('Создать новость') }}</button>
    <a class="btn btn-secondary" href="{{ return }}">Отмена</a>
{{ form_end(form) }}
